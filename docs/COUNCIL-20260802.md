# AI_audio 完善 · 圆桌决策单（2026-08-02）

> 智囊团产出（马斯克 / 乔布斯 / 费曼）· 只讨论决策，不执行
> **状态：已执行**（2026-08-02，Cursor 实施，备份 `E:\VM\AI_audio_bak_20260802`）· 遗留：费曼三实验
> 执行方：Cursor · 项目根：`E:\VM\AI_audio`

---

## 一、总原则（三位共识）

1. **先删后加**：清掉冗余，再谈新功能
2. **核心是「一条链路真正顺」**：登录 → 取流 → 播放 → 歌词 → AI 上下文
3. **为外部调用预留稳定 API 面**（贾维斯/Hermes 以后要接）

---

## 二、行动项（按优先级）

### P0 · 清理与收敛（费曼实验一 + 马斯克「删」）

- [x] 备份后删除：`_archive/`、`prototypes/`、旧前端 `ai-radio_bak`、`old-frontend`
- [x] 前端只留 `ai-radio-v1/`，后端只留 `ai-radio/server/`
- [x] `docs/design/` 从 5+ 份收敛为 1 份活文档（删除 PHASE-2~5、交接等历史稿）
- [x] 清理根目录孤儿文件：`prebuilt.tar.gz` 等（确认无用后删）
- [x] 验证：删完后 `start.bat` 一键启动，全链路不崩

### P1 · 取流链路收敛（马斯克：物理极限）

- [x] 登录态统一：网易云 Cookie / 酷狗扫码，做一个统一会话层（`session.mjs` 强化）
- [x] 取流一条主链 + 一个兜底：`url-smart` 明确主源优先，回退逻辑收敛，避免多层堆叠
- [x] token 过期自动刷新（酷狗已有 `refreshKugouToken`，网易云补齐）
- [x] 播放成功率可观测：加一个指标接口（播放成功/失败计数）

### P1 · API 面稳定化（马斯克：为贾维斯预留）

- [x] 新增稳定接口：
  - `POST /api/queue/add`（歌 id/hash + 来源）
  - `POST /api/player/play`（播队列第 n 首 / 指定 id）
  - `GET /api/player/status`（当前播放/队列）
- [x] 统一响应格式：`{code, data, msg}`，错误码稳定（稳定面 + health/metrics）
- [x] `api/health` 保留，作为外部探活

### P2 · 产品定义收敛（乔布斯）

- [x] 一句话定义：**AI Radio = 有 AI 陪聊的听歌空间**
- [x] 三面贴边面板（左 AI / 右歌单 / 下播放条）→ 收敛：默认只露播放条，AI 与歌单收进按钮/手势
- [ ] 核心体验打磨：点歌到出声零卡顿、歌词同步、封面完整（持续打磨，非本轮阻塞）
- [x] AI 上下文：`/api/chat` 感知当前播放歌曲（body.track 或服务端 player status）

### P2 · 验证实验（费曼）

- [ ] 实验一：清理后连跑 3 天不崩
- [ ] 实验二：找一个没用过的人玩 10 分钟，记录卡点 → 修卡点
- [ ] 实验三：列出想加的新功能，故意搁置一周，一周后还想要再做

---

## 三、圆桌分歧记录（供执行取舍）

| 视角 | 优先级主张 |
|---|---|
| 马斯克 | 取流链路 + API 面（工程命脉） |
| 乔布斯 | UI 收敛 + 一句话定义（产品灵魂） |
| 费曼 | 先验证现状（删了不崩、别人会用）再谈方向 |

**贾维斯建议执行顺序**：P0 清理（含费曼实验一）→ P1 API 面 + 取流收敛 → P2 产品体验。
P0 不依赖任何讨论，可直接开做。

---

## 四、约束

- 本单只供 Cursor 执行，智囊团不写代码
- 清理前必须先备份（可 `git init` 或复制到 `E:\VM\AI_audio_bak_20260802`）
- 执行完由 Cursor 回报结果，需要再议再拉圆桌

---

## 五、执行回报（2026-08-02）

- 备份：`E:\VM\AI_audio_bak_20260802`
- 活文档：`docs/design/AI-RADIO.md`
- 稳定面：`/api/health` `/api/metrics` `/api/queue/add` `/api/player/play` `/api/player/status` → `{code,data,msg}`
- 会话：`ensureFreshSession`（酷狗 refresh + 网易云 `login_refresh`）
- UI：底栏默认常显；左下/右下角 + 边缘线可点开 AI / 歌单
- 人工验证项（费曼实验一～三）留给连跑与真人试用